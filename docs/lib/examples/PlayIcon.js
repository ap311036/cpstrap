import React from 'react';
import { PlayIcon } from 'cpstrap';

const Example = (props) => {
  return (
    <div>
      <PlayIcon type="trailer" />
      <PlayIcon type="preview" />
    </div>
  );
};

export default Example;
